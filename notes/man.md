Debug man pages resolve path with `manpath --debug`

Man looks into `MANPATH` varible for list of directories with man files
Also it looks into relative directory `../man` in each dir defined in `PATH`
