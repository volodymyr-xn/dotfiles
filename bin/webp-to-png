#!/usr/bin/env bash

# Convert WebP image to PNG format

set -euo pipefail

image_file="$1"
image_file_without_extenstion="${image_file%%.*}"

if [ ${image_file: -5} == ".webp" ]; then
  ffmpeg -i "$1" "$image_file_without_extenstion.png"
fi
