#!/usr/bin/env bash

# Display detailed file information (name, owner, permissions)

set -euo pipefail

filename=${1:-"."}

format='Filename: %n (%F)\nOwner: %U(%u):%G(%g)\nPermissions: %A(%a)\n'

stat --printf="$format" "$filename"

