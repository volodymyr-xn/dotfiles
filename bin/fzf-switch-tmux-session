#!/usr/bin/env bash

# Select and switch to tmux session with fzf

set -euo pipefail

tmux_session_name=$(tmux ls -F "#{?@hidden,,#{session_name}}" | grep -v '^$' | sort | fzf)
tmux switch -t "$tmux_session_name"
