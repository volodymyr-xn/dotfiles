#!/usr/bin/env bash

# Select Rails route with fzf and copy path to clipboard

highlight() {
  echo -e "\e[32m$1\e[0m"
}

path_name="$(bin/rails routes | fzf | awk '{print $1}')_path"
echo -n "Copied path: "
highlight "$path_name"
echo "$path_name" | xclip -selection clipboard

