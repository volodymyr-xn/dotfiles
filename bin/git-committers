#!/usr/bin/env bash

# List git contributors by number of commits

set -euo pipefail

git ls-tree --name-only -r HEAD \
  | xargs -n1 git blame --line-porcelain \
  | grep "^author " \
  | sort \
  | uniq -c \
  | sort -nr
