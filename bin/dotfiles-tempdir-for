#!/usr/bin/env bash

# Creates tempdir for sources code or installation fies in "/tmp" directory
set -euo pipefail

name="$1-source"
version=${2:-''}
timestamp=$(timestamp-ms)

if ! [ -z "$version" ]; then
  version="-$version-"
fi

echo "/tmp/$name${version}${timestamp}"
