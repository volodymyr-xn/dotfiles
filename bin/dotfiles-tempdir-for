#!/usr/bin/env bash

# Generate temporary directory path for project sources

set -euo pipefail

name="$1-source"
version=${2:-''}
timestamp=$(timestamp-ms)

if ! [ -z "$version" ]; then
  version="$version-"
fi

echo "/tmp/${name}-${version}${timestamp}"
