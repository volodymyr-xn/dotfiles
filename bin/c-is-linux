#!/usr/bin/env bash

# [[ "$(uname -m)" == "arm64" ]] && echo 0
# [[ "$(uname -m)" != "arm64" ]] && echo 1
#

[[ "$OSTYPE" == "linux-gnu"* ]] && echo true
[[ "$OSTYPE" != "linux-gnu"* ]] && echo false
