#!/usr/bin/env bash

# Replace file with symlink to /dev/null for development

set -euo pipefail

filename=$1
full_path_to_file=$(readlink -f $filename)

rm $filename
ln -s /dev/null $full_path_to_file
