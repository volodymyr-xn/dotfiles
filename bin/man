#!/usr/bin/env bash

# Display man pages with limited width (max 80 chars)

set -euo pipefail

columns=$(tput cols)
max_width=80

if [ "$columns" -lt "$max_width" ]
then
  man_width="$columns"
else
 man_width="$max_width"
fi

MANWIDTH="$man_width" /usr/bin/man "$@"
