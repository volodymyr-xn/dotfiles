#!/usr/bin/env bash

set -euo pipefail

columns=$(tput cols)
max_width=70
# half_of_screen=$(( ($COLUMS / 2) - 10 ))

if [ "$columns" -lt "$max_width" ]
then
  man_width="$COLUMNS"
else
 man_width="$max_width"
fi

MANWIDTH="$man_width" /usr/bin/man "$@"
