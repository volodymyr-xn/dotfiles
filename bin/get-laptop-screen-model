#!/usr/bin/env bash

# Get laptop screen model name from EDID

set -euo pipefail

match_string="Alphanumeric Data String"

sudo get-edid 2> /dev/null \
  | edid-decode \
  | grep "$match_string" \
  | sed  "s/$match_string//" \
  | sed  "s/:\s//"
