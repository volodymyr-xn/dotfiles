#!/usr/bin/env bash

# Decrease macOS volume by 10%

current=$(osascript -e "output volume of (get volume settings)")
new=$((current - 10))
if [ $new -lt 0 ]; then new=0; fi

osascript -e "set volume output volume $new"

