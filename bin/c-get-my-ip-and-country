#!/usr/bin/env bash

# Get public IP address and country

set -euo pipefail

public_ip=$(curl -s https://ipinfo.io/ip)
country=$(curl -s https://ipinfo.io/$public_ip/country)

echo "Public IP:  $public_ip"
echo "Country:    $country"
