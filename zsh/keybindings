# CTRL-o runs fzf switch tmux session
# bindkey -s "^o" "tmux split-window -h 'fzf-switch-tmux-session'\n"
# bindkey -s "^o" 'fzf-switch-tmux-session\n'

# Git status
# ctrl-f
# bindkey -s "^f" 'git status\n'
bindkey -s "^f" 'git status\n'

# Git diff staged files
# ctrl-e
# bindkey -s "^o" 'git diff --staged\n'

# Clears console
# ctrl-m
#bindkey -s "^b" 'clear\n'

# Run rails console
# ctrl-o
bindkey -s "^o" 'bin/rails c\n'

# Run claude in vertical tmux split
# ctrl-n
bindkey -s "^n" $'claude\n'

bindkey "^e" forward-word
bindkey "^v" backward-word
# bindkey "^d" backward-delete-word
# bindkey "^v" delete-word

bindkey "^d" delete-word

# bindkey -s "^x" 'eza -alh\n'
# bindkey "^e" beginning-of-line
# bindkey "^]" end-of-line
# bindkey "^u" backward-kill-line
# bindkey "^d" backward-kill-line

# # bindkey -s "^i" 'git diff --staged\n'
# bindkey -s "^o" 'git diff --staged\n'
#
# # Git diff with "ctrl-m"
# bindkey -s "^x" 'git diff\n'
#
# bindkey -s "^n" 'clear\n'


# Experimental
# bindkey -s "^n" 'cd ~/dev_notes\n'
